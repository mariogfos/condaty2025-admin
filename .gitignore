# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
.kilocode
.kilocode/

# testing
/coverage
# next.js
/.next/
/.next-build/
/out/
/nogit
nogit/
/test-results
test-results/
prompt_aliases.json

*.log*

# production
/build
.env*
!.env.example
/.env.development
.env.production
/context
# misc
.DS_Store
*.pem


# env files (can opt-in for committing if needed)
# .env*

# vercel
.vercel


.prettierrc

# typescript
*.tsbuildinfo
next-env.d.ts

#custom
.devdbrc
/.env.development
package-lock.json
/.env.development
/.env.production

WARP.md

# Ignore code scanner and editor settings
/.vscode/
/.scannerwork/
/.codex/

# env 
/.env.development

#package-lock.json
package-lock.json
pnpm-workspace.yaml

# Los worktrees locales. Uno se commiteo por error como gitlink (modo
# 160000) y viajo en `test`: sin entrada en `.gitmodules`, cualquier clon
# se queda con una referencia a un submodulo que no existe.
.worktrees/
